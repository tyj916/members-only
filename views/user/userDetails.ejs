<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= username %></title>
</head>
<body>
  <%- include('../partials/header.ejs') %>

  <main>
    <div class="user-details">
      <h1><%= username %></h1>
      <h2><%= first_name + ' ' + last_name %></h2>
      <div class="membership-status">
        <h3><%= membership_status %></h3>
        <% if (locals.user && user.username === username) { %>
          <a href="/join-member" class="btn-link">Change Membership</a>
        <% } %>
      </div>
    </div>
  
    <div class="posts">
      <% if (posts.length === 0) { %>
        <p>No posts yet</p>
      <% } else { %>
        <% posts.forEach(post => { %>
          <%- include('../partials/postDetails.ejs', {post: post}) %>
        <% }); %>
        <p class="end-of-posts"> - End - </p>
      <% } %>
    </div>
  </main>

  <%- include('../partials/nav.ejs'); %>
</body>
</html>